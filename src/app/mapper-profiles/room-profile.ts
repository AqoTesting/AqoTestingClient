/*import { AutoMapper, ProfileBase } from '@nartc/automapper';
import { Room, RoomEdit } from '../entities/room.entities';

export class RoomProfile extends ProfileBase {
  constructor(mapper: AutoMapper) {
    super();
    mapper.createMap(Room, RoomEdit);
  }
}*/